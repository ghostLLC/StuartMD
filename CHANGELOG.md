# 更新日志 / Changelog

本文件记录 StuartMD 的版本变更，便于开发与发布对照。

## [2.8.0] - 2026-09-17

### PDF 阅读体验
- **工具栏改图标**：适应（适应高度）、顺时针旋转、缩放、翻页
- **双页显示**：左右并排两页，翻页按 2 页步进；双页时自动按两页宽度适配
- **Ctrl+滚轮** 缩放显示
- **标黄默认关闭**；颜色固定浅黄；工具栏仅保留标黄开关图标
- **擦除**：去掉独立擦除键——标黄关闭时单击高亮可选中，右键「擦除此高亮」
- **清除**：图标 + **两次确认**
- **去掉复制按钮**（与系统选中右键复制重复）
- **选区观感**：去掉文字层 `opacity:0.25`，改用与高亮同系的**浅琥珀选区**，减轻蓝色与文字层割裂感

## [2.7.1] - 2026-09-17

### 底层二次性能优化
- **Markdown 块 HTML 缓存**：同源块不再重复 `md.render`（切标签 / 撤销 / 大段删除后重绘显著更快）
- **splitMarkdownBlocks 结果缓存**：相同全文不重复切块
- **KaTeX 后处理**：更精确识别 `$…$` / `$$`；前 6 块同步、其余 idle，避免公式文档首屏卡死
- **块编辑 / 撤销路径**：去掉多余的整篇 `splitMarkdownBlocks`，交给增量 diff
- **标签栏事件委托**：关闭/切换/拖拽不再为每个标签绑监听
- **最近文件**：已是队首则不写盘；标签渲染用 DocumentFragment
- **状态栏字数**：撤销后立即刷新；其余仍节流

## [2.7.0] - 2026-09-17

### 性能（阅读模式 + 全局操作）
- **块渲染**：仅当 HTML 含脚本/事件属性时才做消毒，普通段落跳过全树扫描
- **悬停手柄**：命中检测限频约 30fps；拖选/选区仍直接跳过
- **字数统计 / 大纲**：节流；大纲改为事件委托，重建不再绑 N 个监听
- **脏标记**：输入时只修补标签小圆点，不再整条重建标签栏
- **标签标题**：文件夹集合缓存，避免 O(n²)
- **预览调度**：合并重复 scheduleRender；超长文档 debounce 更长
- **查找**：本文件高亮上限 400，避免超大命中卡死
- **文件树 / 最近**：高亮只改前后节点；最近列表事件委托
- **会话写盘**：debounce 加长到 900ms
- **CSS**：`.md-block { contain: layout style }` 缩小重排范围（不用 content-visibility，避免 WebView2 滚动异常）

## [2.6.1] - 2026-09-17

### 新增 / 修复
- **示例文档补全**：恢复完整语法演示、快捷键、PDF、外观与「性能测试段落」；内置 fallback 同步用仓库 `samples/示例文档.md`
- **阅读模式大段选中删除卡顿**（性能）：
  - 拖选时不再每帧做块命中检测（原 `getBoundingClientRect` 全量扫描是主因）
  - 新增预览选区 **Delete/Backspace 快路径**：多块删除只改 Markdown 源一次 + 单次渲染，不再进 contenteditable 逐键重排
  - 大纲重建节流 80ms；悬停 fallback 扫描块数上限
  - 无 editing 时跳过 `getActiveEditingBlock` 清理路径

## [2.6.0] - 2026-09-17

### 修复 / 启动体验
- **去掉启动 cmd 黑框**：注册关联时不再调用 `cmd /C ie4uinit`（2.5.2 为刷新资源管理器引入，观感差）
- **缩短白屏**：高亮 / Mermaid / PDF / 插件 / 音乐脚本改为 `defer`；关联注册与插件加载延后，不挡启动
- **启动不再先闪主页**：用 boot splash 盖住，按策略直达目标页

### 启动策略（按你的要求）
1. **安装后首次**：直接打开示例页（不经过主页）
2. **示例页被关闭后**：下次启动进**主页**（不再自动开示例）
3. **有上次会话**：恢复标签栏打开的文件 + 左侧上次目录
4. 命令行 / 文件关联打开的文件优先
5. 老用户（已有最近文件或目录）迁移时视为已关闭示例，直接进主页/会话

### 其他
- 检查更新的 PowerShell 使用隐藏窗口 + `CREATE_NO_WINDOW`
- 关闭标签 / 打开文件夹 / 切标签 / 关窗口时写入会话（tabs + active + folder + sample_dismissed）

## [2.5.2] - 2026-09-17

### 修复
- **PDF 打开后整页空白**：`openPdf` 只调用了 `fitToWidth(false)` 计算缩放，**没有调用 `renderAll()`**，导致工具栏能显示总页数、内容区却不绘制任何页面
- 打开 PDF 后立即绘制前两页；IntersectionObserver 失效时 400ms 兜底再画
- 占位符阶段不再对每一页重复 `getPage(1)`（长报告更轻）
- 单页渲染失败会显示「第 N 页渲染失败」，不再静默空白
- PDF.js worker 路径按页面 URL 解析，关联启动时更稳

## [2.5.1] - 2026-09-17

### 修复
- **PDF 出现在系统「打开方式」**：此前文件关联只注册了 `.md/.markdown/.mdown/.mkd`，未写入 `.pdf`
- 注册逻辑补充：
  - `OpenWithProgids\StuartMD.PDF`（不抢系统默认 PDF 阅读器）
  - `Applications\stuartmd.exe\SupportedTypes`（.pdf / .md / .txt 等）
  - 独立 ProgID `StuartMD.PDF`
- 安装包增加 `fileAssociations`（md Editor + pdf Viewer）
- 应用启动时自动刷新一次关联（升级后路径变化也能跟上）
- 设置页状态文案分别显示 Markdown / PDF 注册情况

### 使用
- 设置 → 默认打开方式 → **注册 Markdown / PDF 打开方式**
- 对 PDF 右键 → 打开方式 → 选择 StuartMD（若列表未刷新，可稍等或重开资源管理器）

## [2.5.0] - 2026-09-17

### 新增
- **灰色主题**：介于浅色与深色之间；黑底改为中灰，白字压暗，减轻夜间刺眼
- 主题菜单 / 设置 / 三语词条同步加入「灰色 / Gray」

### 变更
- **分栏布局**：左侧渲染内容，右侧 Markdown 源码
- 分栏拖动条按右侧源码宽度调整（20%–75%）

## [2.4.1] - 2026-09-17

### 变更
- **分栏布局**：左侧为渲染内容（预览），右侧为 Markdown 源码（原先左右相反）
- 分栏拖动条改为按右侧源码宽度调整（20%–75%）

## [2.4.0] - 2026-09-17

### 中期目标准备（AI 接口）
- 新增稳定宿主门面 `window.StuartAgent` / `window.Stuart`：文档读写、大纲、块、选区、撤销、工具注册
- 插件 API 增加 `StuartPlugin.registerTool` / `agent()`
- 后端新增：`stuart_get_capabilities`、`ai_home`、`memory_*`、`stuart_search_md`、`stuart_workspace_files`
- AI 记忆目录：`%APPDATA%\StuartMD\ai\memory\`（键名白名单 + 大小限制）
- Agent 写入文档默认清洗 `<script>` / `on*` / `javascript:`

### 安全加固（不改交互）
- `open_url` 仅允许 `http://` / `https://`
- 插件源码读取限制在插件目录内
- 导出 HTML 文件名消毒，阻断 `../` 路径穿越
- 壁纸 base64 体积上限；相对图片路径阻断 `..` 逃逸
- 设置读写加锁；无变更时不再反复写盘
- 预览/ Mermaid SVG 渲染后剥离脚本类节点

### 性能 / 稳定性
- 自动保存竞态：切换文档后不再误标「已保存」
- 打开文档加序号防竞态；文件夹搜索可中断 + 分批 + 优先走后端
- 撤销栈增加约 4MB 字节上限；块编辑 document 监听不再泄漏
- 重建预览后清理失效查找高亮；PDF 切换时销毁 worker
- 目录树遍历节点上限；非 UTF-8 文件不再静默变成空文档

## [2.3.1] - 2026-09-17

### 修复 / 变更
- 块手柄菜单不再遮挡正文：图标栏改为 **4 列 × 3 行**，菜单宽度 220px → **168px**
- **默认页宽**：侧栏与正文间距加大（内容 max-width 780px + 左侧 80px 留白），手柄与菜单更不易压字
- **较宽 / 全宽**：块菜单位置对齐飞书——落在**选中内容（行/段/选区）下方**，避免挡住选中文字
- 默认页宽若左侧空隙不够，菜单也会自动改到选中块下方

## [2.3.0] - 2026-09-17

### 变更
- `Ctrl+Shift+Z` = 撤销（同 Ctrl+Z）；`Ctrl+Shift+Y` = 重做（同 Ctrl+Y）
- 块手柄菜单改到**左侧**，尽量不挡正文，也避开侧栏
- 右上角新增**页宽**：默认 / 较宽 / 全宽（主题与「更多」之间）
- 仓库清理：移除 pywebview/Inno 旧构建目录与入口（`dist/`、`build/`、`main.py` 等），只保留 Tauri 维护线

## [2.2.1] - 2026-09-17

### 修复
- 新增内容可 Ctrl+Z 撤销：变更前状态立即入栈；撤销前先提交未失焦的块编辑
- Ctrl+Y / Ctrl+Shift+Z 重做同步修复；预览强制重建，避免撤销后界面不更新

## [2.2.0] - 2026-09-17

### 新增
- 查找栏明确「搜索」按钮；Enter 首次搜索、再次 Enter 下一个
- 查找范围：本文件 / 同级文件夹（仅 md，限扫文件数与结果数）
- 右键插入菜单：图像、链接引用、分割线、表格、代码块、公式块、段落上下

### 修复
- 查找高亮不再因重复 Enter 膨胀（先清理 mark 再标记，导航复用结果）
- 列表行首 Backspace 先取消分点，不直接并入上一行

## [2.1.0] - 2026-09-17

### 变更
- Mermaid 图表 / 块级 LaTeX 不再显示块手柄
- 代码块支持**单击**进入源码编辑
- 编辑过程中手柄不再强制隐藏，单击后仍可唤起

## [2.0.9] - 2026-09-17

### 修复
- 单击编辑结束后若光标残留，块手柄无法再唤起：仅在真正聚焦编辑时屏蔽手柄；失焦后自动清理残留 .editing

## [2.2.1] - 2026-09-17

### 修复
- 新增内容可 Ctrl+Z 撤销：变更前状态立即入栈；撤销前先提交未失焦的块编辑
- Ctrl+Y / Ctrl+Shift+Z 重做同步修复；预览强制重建，避免撤销后界面不更新

## [2.2.0] - 2026-09-17

### 新增
- 查找栏明确「搜索」按钮；Enter 首次搜索、再次 Enter 下一个
- 查找范围：本文件 / 同级文件夹（仅 md，限扫文件数与结果数）
- 右键插入菜单：图像、链接引用、分割线、表格、代码块、公式块、段落上下

### 修复
- 查找高亮不再因重复 Enter 膨胀（先清理 mark 再标记，导航复用结果）
- 列表行首 Backspace 先取消分点，不直接并入上一行

## [2.1.0] - 2026-09-17

### 变更
- Mermaid 图表 / 块级 LaTeX 不再显示块手柄
- 代码块支持**单击**进入源码编辑
- 编辑过程中手柄不再强制隐藏，单击后仍可唤起

## [2.0.9] - 2026-09-17

### 修复
- 单击编辑结束后残留光标 / editing 状态不再永久压住块手柄
- 无焦点的编辑态会自动提交并恢复渲染，悬停手柄可再次唤起

## [2.0.8] - 2026-09-17

### 变更
- 列表 / 表格手柄按**行**定位（悬停到 li / tr 时对齐该行）
- 选中高亮改为**灰色**，并按主题适配（浅色/深色/羊皮纸/小黄人/壁纸）
- 选中浮动工具栏改到选区**下方**

## [2.0.7] - 2026-09-17

### 修复
- 块手柄移入预览面板并改用 fixed 定位，列表 / 代码 / 公式 / 表格上也能稳定出现

## [2.0.6] - 2026-09-17

### 修复
- 双击代码块 / 行内代码 / LaTeX / Mermaid 可进入源码编辑
- 块手柄命中率：document 级追踪 + elementFromPoint + 垂直带兜底，鼠标所在行必出手柄

## [2.0.5] - 2026-09-17

### 新增
- Ctrl+Z 撤销、Ctrl+Y / Ctrl+Shift+Z 重做（覆盖预览/块编辑/源码）

### 变更
- 块手柄合并为单一 T ⠿ 按钮
- 手柄展开的块菜单：鼠标离开手柄与菜单后自动收起
- 欢迎示例文档随版本同步（版本号 + 完整功能介绍）

## [2.0.4] - 2026-09-17

### 修复
- 双击进源码编辑后失焦立即回渲染（原先内容未变会跳过重绘，一直停在源码）
- 块外点击也会提交编辑
- 悬停手柄改为延迟隐藏，移向手柄的间隙不再消失

## [2.0.3] - 2026-09-17

### 修复
- 悬停手柄：移向手柄的间隙不再消失（近接保护 + 手柄自身 hover）
- 手柄左移，不再遮挡正文
- 出现选中浮动栏时同时隐藏块手柄

## [2.0.2] - 2026-09-17

### 修复 / 体验（飞书对齐）
- 悬停块手柄未绑定导致不显示；改为 T + 拖动点
- 块菜单：类型宫格 + 缩进和对齐子菜单 + 剪切/复制/副本/删除
- 选中高亮改为浅蓝底深色字（深色主题用深蓝底白字），避免白字突兀

## [2.0.1] - 2026-09-17

### 修复
- 大纲点击跳转：预览重建后按标题索引定位当前节点（原先绑到已卸载 DOM，点了不跳）
- 从源码模式跳转时先切分栏，再滚动；目标标题短暂高亮

## [2.0.0] - 2026-09-17

### 变更
- **大版本**：Tauri 为唯一维护线，版本推进至 2.0.0
- **图标**：靛蓝底 + 白字 S + 蓝色强调条；小尺寸 BMP ICO；安装包指定 `installerIcon`
- 安装目录：`%LOCALAPPDATA%\StuartMD`（NSIS currentUser）

## [1.14.0] - 2026-09-15

> 维护策略：**后续只更新维护 Tauri 轨道**；pywebview Stable 停留在 1.13.1。

### 新增（Tauri）
- 最近打开列表：读写文件、打开文件夹自动记录
- 导出 HTML：系统保存对话框 + 写盘
- 新建空窗口、在资源管理器中显示
- 插件启用/禁用持久化（`plugins_disabled`）
- CLI / 文件关联启动：`startup_file` 注入 `get_app_info`
- 打包内置示例与欢迎文档（`resources/samples`）
- NSIS 安装：简体中文/英文、可选语言、当前用户安装

### 修复（Tauri）
- 设置 `schema_version` 迁移与默认值补全（与 Stable 对齐）
- `resolve_asset` 返回 `file://` URI，预览相对路径图片可用
- PDF 打开时附带 sidecar 批注
- 桥接补齐：`get_recents` / `open_path` / `open_new_window` / `export_html` / `set_plugin_enabled` / `clear_wallpaper` / `reveal_in_explorer`
- `withGlobalTauri`：系统打开/保存/文件夹对话框真正可用
- 版本号统一为 1.14.0（此前 Rust 内嵌仍为 1.12.1）

## [1.13.1] - 2026-09-15

### 新增
- 内置欢迎文档：冷启动无指定文件时自动打开功能介绍（含 GitHub 仓库地址）
- 设置 → **检查更新**：查询 GitHub Releases，发现新版本可打开下载页
- 配置 `schema_version` 与自动迁移：升级时完整保留主题、语言、插件、壁纸、最近文件等

### 变更
- 安装向导明确支持**自定义安装路径**（未禁用目录页；静默安装可用 `/DIR=`）
- 版本号策略：功能变更递增，不再长期停留在 1.0.0

### 文档
- 欢迎 Markdown 写入仓库地址与安装说明
- `docs/PLUGINS.md` 插件开发指南

## [1.1.0] - 2026-09-15

### 新增
- 横版 PDF 适配：自动识别、适应宽/高、旋转
- 插件系统（`%APPDATA%\StuartMD\plugins`）与 `docs/PLUGINS.md`
- 壁纸主题：上传图片自动提取主色
- 三语：简体中文 / 繁體中文 / English

## [1.0.0] - 2026-09-15

### 新增
- 首个正式版 StuartMD（原 MiniTypora / Stuart 演进）
- Markdown 阅读 / 分栏 / 源码；公式、Mermaid、代码高亮
- PDF 阅读与标黄批注（竖版）
- 自动保存、多窗口、文件关联、Inno 安装包
- 主题：浅色 / 深色 / 羊皮纸 / 小黄人 / 毛玻璃
- 设置持久化至 `%APPDATA%\StuartMD`
