# 更新日志 / Changelog

本文件记录 StuartMD 的版本变更，便于开发与发布对照。

## [3.1.8] - 2026-09-21

### 修复：AI 窗口拉高出现底部空白
- 正文区去掉 `max-height` 限制，拉高面板时正文跟随占满
- 统一 flex 布局：头/引文/操作条固定，正文 `flex:1` 撑满剩余高度

## [3.1.7] - 2026-09-21

### 交互
- AI 入口改为**纯图标**（MD / PDF 同一星形图标，无文字）；悬停提示「AI 问答」

## [3.1.6] - 2026-09-21

### 文案与布局
- PDF / Markdown 入口按钮文案由「讲解」统一改为「**问答**」（AI 问答）
- 面板标题「AI 问答」；提示同步；按钮禁止换行（修复 PDF 条上「讲/解」竖排）

## [3.1.5] - 2026-09-21

### 图标设计
- **重绘 MD / PDF 文件图标**：纸片卡片 + 品牌色侧栏 + 折角 + 大号品牌色字标（靛蓝 MD / 正红 PDF），更有产品感
- 小尺寸优先：字标放大、纸片+色条结构清晰
- PDF 未刷新：安装后强制覆盖 `file-pdf.ico` 并清空 iconcache（ProgId `PDF`）

## [3.1.4] - 2026-09-21

### 修复：图标字形上半与白底重叠
- 去掉卡片顶部半透明高光（白字上半被冲淡、难辨认）
- 实心色底 + 白字 + 深色描边，文件夹 16–32px 对比更强

## [3.1.3] - 2026-09-21

### 修复：PDF 仍显示应用图标；MD 角标过小
- **根因**：`.pdf` 默认 ProgId 是 **`PDF`**（安装器创建），其 `DefaultIcon` 为 `stuartmd.exe,0`，资源管理器回退到应用图标
- 现对 **`PDF` / `pdffile` / `StuartMD.PDF` / `.pdf\DefaultIcon`** 统一写入 `file-pdf.ico,0`
- **MD / PDF 图标重绘**：大号加粗角标（文件夹 16–32px 可辨认），色块底 + 白字
- 覆盖 `MSEdgePDF` 图标仅当该 ProgId 已存在（不凭空创建）

## [3.1.2] - 2026-09-21

### 修复：PDF 文件图标
- **根因**：资源管理器绘制 `.pdf` 跟随默认程序（常见 MSEdgePDF）图标，而非 StuartMD.PDF 的 DefaultIcon
- 写入 `Software\Classes\.pdf\DefaultIcon` → `file-pdf.ico,0`
- ICO 改为经典多尺寸 BMP 帧，避免 shell 加载失败回退为 stuartmd.exe 图标
- DefaultIcon 统一带 `,0`；注册后刷新关联（SHChangeNotify）

## [3.1.1] - 2026-09-21

### 图标
- PDF 标注条「讲解」图标与 Markdown 选区栏**统一**（星形 + 两行）
- **独立文件图标**：`.md` 蓝靛「MD」文档图标，`.pdf` 红色「PDF」文档图标（与应用图标区分）
- 资源随安装包释放 `file-md.ico` / `file-pdf.ico`；注册打开方式时写入对应 `DefaultIcon`

## [3.1.0] - 2026-09-21

### 新增：PDF 选区 AI 讲解
- PDF 中选中文字后，标注小工具条增加「**讲解**」（与高光/评论同级），快捷键 **Alt+E** 同样可用
- 上下文自动拼装：**选区 + 当前页文本 + 前后页**（页码写入提示词，可引用 P{n}）
- 与 Markdown 共用讲解面板：聊天记录、追问、自定义风格、长度反馈、错误提示
- 文本层缓存：已渲染页优先用缓存，邻页按需抽取；扫描版无文本层时会提示限制
- **只讲解，不改写** PDF

## [3.0.8] - 2026-09-21

### 设置
- **设置窗口默认加宽加高**（约 880×820，随窗口自适应），适配 AI 等更多配置项
- **可拖拽调整大小**（右下角手柄 / CSS resize），尺寸会记住
- **可缩放界面**：标题栏 − / ＋ / 重置，或在设置内容上 **Ctrl+滚轮**；缩放比例会记住
- 标题栏可拖动设置卡片；「重置」恢复默认尺寸与 100% 缩放

## [3.0.7] - 2026-09-21

### AI 讲解面板
- **聊天记录**：讲解与追问在同一面板内保留「你 / AI」消息流，追问不再覆盖上一轮
- **窗口位置稳定**：追问/再讲时**不再**飞到左上角；用户拖过的位置会被记住
- **思考动画**：等待首字时显示轻量三点动画「正在思考…」
- **异常提示统一**：密钥无效 / 模型名或 Base 有误 / 网络失败 / 限流 → 面板内分类文案 + toast，并引导设置或重试
- **拖拽**：应用窗口内可自由拖（含贴边）；**说明**：WebView 内容无法画出宿主窗口外，若需真正「飞出主窗口」需独立原生浮窗（后续可选）

## [3.0.6] - 2026-09-21

### 新增：自定义风格 + 首次回答长度反馈
- **设置 → AI → 自定义风格**（不叫 AGENTS.md）：回答长度（简短/适中/详细）、对话语气（中性/亲切/专业/学术）、自由文本风格说明、**长度提示**
- **首次讲解**结束后出现图标反馈：**太短 / 满意 / 太长**
- 点选后会把**轻量长度约束**写入「自定义风格 → 长度提示」（用户可见、可改），并注入系统提示词；约束刻意从轻，避免束缚模型
- 反馈会镜像记入 AI 记忆活动，便于后续「越用越好」
- 追问回合不重复弹出首次反馈条

## [3.0.5] - 2026-09-21

### 修复
- **公式点几下就坏掉**：含行内 KaTeX 的段落单击会进入可编辑，`htmlToMarkdown` 把 MathML/HTML 双份 DOM 拼成 `E=mc2E=mc^2…` 写回源码
  - 单击**不再**对公式/代码/图表进入 WYSIWYG 编辑；双击进**源码编辑**（保留 `$...$` / `$$...$$`）
  - `htmlToMarkdown` 在 KaTeX 根节点只输出原始 TeX，不再递归视觉 DOM
  - 若转换丢失数学定界符，提交时**回退原块源码**

### 体验
- **AI 讲解面板可拖拽调整宽高**（右下角手柄 + CSS resize），尺寸会记住
- AI 面板 / 顶栏模型菜单 / 设置「AI 模型」区块视觉统一（紫色强调、状态徽章、更清晰的表单）

## [3.0.4] - 2026-09-21

### 修复
- **选区易丢 /「请先选中要讲解的内容」**（承接 3.0.3）
  - 划选后不再被单击进入块编辑抢走选区
  - 选区快照缓存，讲解时实时选区空了仍可用
- **标注后的 PDF 在 Edge 打不开**
  - **不再对整份 PDF 做 `decompress()`**（重写流结构是 Edge/PDFium 拒读的主因）
  - 中文评论 `Contents` 改为 **UTF-16BE + BOM**，避免非法字符串
  - 批注增加 `/P` 页引用；过滤非法坐标
  - **写出后先重载校验**，失败则**不覆盖**原文件
  - 加密 PDF 直接拒绝写入，避免写坏
  - MediaBox 支持从 Pages 父节点继承，坐标更准

## [3.0.3] - 2026-09-21

### 修复：选区易丢 +「请先选中要讲解的内容」
- **根因**：划选结束后预览区单击会进入块编辑，约 200ms 后抢走选区；点「讲解」时读到空选区
- **划选保护**：存在文本选区或选区条时，**不再**单击进入块编辑；划选刚结束的 600ms 内忽略误点击
- **选区缓存**：选区条出现时 / selectionchange 时快照选中文字；讲解优先读实时选区，空了再用缓存（约 2 分钟内仍可讲解）
- **点击保护**：选区条/讲解面板/模型菜单/设置内的点击不再收起选区条；靠近工具条的误点会尽量保住选区
- **getSelectionInfo**：放宽预览选区判定，并回退到选区缓存

## [3.0.2] - 2026-09-21

### 修复
- **点击「讲解」完全无反应**：`app.js` 调用了未定义的 `triggerAiExplain()`，导致 ReferenceError、界面无任何提示
  - 改为正确调用 `StuartAIUI.triggerExplain`
  - 选区条讲解按钮在 `StuartAIUI.bindAll` 中二次直接绑定
  - 讲解启动失败时必有 toast / 面板错误提示（不再静默）

## [3.0.1] - 2026-09-21

### 修复
- **「模型」无法输入**：标题栏是窗口拖拽区，AI 下拉里的输入框/下拉在 WebView2 中可能点不进、选不中
  - 为标题栏与 AI 菜单内的 `input/select` 强制 `no-drag` + 可选中文本 + 可点击
  - 菜单 `mousedown` 不再冒泡到拖拽区，打开后自动聚焦服务商/模型框
- **输入被冲掉**：刷新 AI 配置时不再覆盖**正在编辑**的模型 ID / Base / 快捷键
- **快捷键误伤**：光标在输入框内时不再触发 Alt+E 讲解；应用全局快捷键会避开表单输入
- 设置中模型 ID / API Base 明确可手动输入；切换服务商后自动填入并选中便于改写

## [3.0.0] - 2026-09-21

### 新增：选区 AI 讲解（中期 AI 第一期）
- **Markdown 选区讲解**：选中文字后，选区工具条出现「讲解」，或按 **Alt+E**（可在设置中改）
- **浮动讲解面板**：引用选区 + 流式回答，支持取消 / 复制 / 记入记忆 / 追问 / 再讲一次；**只讲解，不改写文档**
- **右上角「模型」图标**：快速切换服务商、模型 ID、思考深度（快速 / 均衡 / 深入）
- **设置 → AI 模型**：启用开关、服务商、API Base、模型、**API Key（本机 DPAPI 加密）**、快捷键自定义、上下文范围、记忆策略
- **内置 OpenAI 兼容预设**：DeepSeek / 通义千问 Qwen / Kimi / 智谱 GLM / 自定义
- **上下文策略**：默认「邻近」（选区 + 章节 + 前后块）；可切换「结合全文」（按字数截断）
- **记忆默认学习**：默认 `always`（记录讲解活动与偏好线索）；可改为「询问 / 关闭」；讲解时可注入 `user_profile` 记忆
- **安全**：密钥不进 settings.json；讲解输出用安全 Markdown 渲染；快捷键默认避开 Ctrl+Shift+Z 撤销策略

### 说明
- PDF 选区讲解与更多厂商 Adapter 在后续 3.1.x / 3.2.x
- 配置项写入 settings schema **v4**（`ai` 对象）；旧设置自动迁移

## [2.9.10] - 2026-09-17

### 修复
- **评论蓝线与下划线对齐**：同一套几何（行框 93% + 3px、线宽 2.5px、像素对齐），仅颜色为蓝；即「评论 = 蓝色下划线」

## [2.9.9] - 2026-09-17

### 示例文档
- 标题去掉「（对齐飞书）」「（Mermaid）」
- 「标黄」统一改为「标注 / 高光」

### PDF 标注
- **下划线更靠下**（行框约 93% + 3px），避免压字
- **线宽彻底统一**：Canvas 上按颜色+像素行合并后再画，避免叠线变粗
- **清除标注**：默认**不**清评论；弹窗可勾选「同时清除评论」

## [2.9.8] - 2026-09-17

### 修复
- **清除标注确认框**：弹出时强制内联居中样式；只弹 **一次** 确认
- **下划线 / 评论线粗细不均**：改为 **Canvas 绘制**（固定 2.5px），不再用 DOM 线叠加；高光/下划线/删除线/评论视觉统一
- 重绘后清掉残留 zoom，避免线被整体放大变粗

## [2.9.7] - 2026-09-17

### 修复
- **清除全部标注**：Tauri WebView 里原生 `confirm()` 可能静默无效；改为**应用内确认弹窗**，必弹 **两次**
- **评论/下划线深度与粗细**：
  - 线位置改为行框高度的固定比例（约 **86%**），不再贴 `bottom`，避免行框高度不一导致「深浅不一」
  - 线宽按设备像素量化到 **2.5px**
  - 删除线固定在行框 **54%** 高度
  - 原生批注 id 去重，避免重复叠线变粗

## [2.9.6] - 2026-09-17

### 会话与窗口
- 清除标注提示改为 **「已清除全部标注」**；清除前 **两次确认**
- 记住窗口 **普通 / 最大化 / 全屏** 与位置大小，启动时恢复
- 直接启动软件时：优先打开 **上次的文件标签**（`session.tabs` / `last_open_files`），无记录再走示例/主页

### PDF
- **Ctrl+滚轮**：对滚动区使用 CSS `zoom`（内容与纸张同步缩放），滚轮结束后再高清重绘，观感更接近 Edge
- **标注粗细稳定**：高光统一透明度 0.32；下划线/删除线固定 2.5px；删除线固定在行框 54% 高度

## [2.9.5] - 2026-09-17

### 修复：多行评论蓝线变粗
- **原因**：多行选区可能被合成一个很高的矩形，下划线画在整块底部；或与填充样式叠在一起
- **修复**：
  - 合并只在**同一视觉行**内进行，多行始终拆成多条线
  - 视觉线改为独立子节点，**固定 2.5px**，与行数无关
  - 点击热区透明；仅「高光」有底色填充

## [2.9.4] - 2026-09-17

### PDF 标注细节
- **高光合并**：同一行上因空格/英文断开的 `getClientRects` 会合并成一块（含间隙），多行仍是同一标注；单击任一片段即选中**整块**并弹出操作
- **下划线**：更靠下（略出框底）、约 **2.8px**，不再压在字上
- **删除线**：下移到约 54% 行框高度，更接近字心；约 **2.8px**
- **默认色**：高光淡黄 · 下划线黄绿 · 删除线鲜红 · 评论蓝下划线

## [2.9.3] - 2026-09-17

### PDF 标注交互
- **Ctrl+滚轮**：先对已渲染页面做 CSS 缩放，再防抖重绘，避免中间出现细长空白占位
- **评论呈现**：被评论内容为**蓝色下划线**；鼠标悬停出现**跟随浮窗**显示评论
- **选中后小工具条**（鼠标旁）：高光 / 下划线 / 删除线 / 评论
- 右上角**笔图标 = 调整标注颜色**（不再开关高光模式）；**取消**独立颜色点按钮
- 标注类型支持 highlight / underline / strike / comment；写入 PDF 时映射为 Highlight / Underline / StrikeOut / Text

## [2.9.2] - 2026-09-17

### PDF 评论与高亮交互
- **评论浮窗**：改为 WPS 式**可拖动**浮动窗（无遮罩），可一边看正文一边写；标题栏拖动；保存后可不关窗
- **高亮可左键选中**：标注关闭时，左键单击高亮/评论标记即选中，弹出改色 / 评论 / 删除
- **层序修正**：文字层在下可选中，标注层在上接收点击（标注开启时文字层仍可框选）

## [2.9.1] - 2026-09-17

### PDF 评论：针对任意选中内容
- **不必先高亮**：在 PDF 里选中任意文字 → **右键** →「**添加评论**」
- 右键菜单同时提供「高亮选中」（可选色）
- 纯评论显示为**蓝色竖条 + 💬**，不铺满整段底色，避免与高亮混淆
- 对已有高亮仍可右键：编辑/添加评论、改色、擦除
- 写入 PDF 时评论以原生便签（`/Text`）导出，WPS/Edge 可见

## [2.9.0] - 2026-09-17

### PDF 标注体系
- **行框对齐**：高亮矩形恢复为选区**完整上下边界**，不再整体下移
- **选区与标注分离**：选中为**蓝色**；标注为浅色荧光（黄/绿/蓝/粉/橙/紫）
- **多色标注**：工具栏颜色点可选色；右键标注可改色
- **评论/批注**：右键 →「添加/编辑评论」；有评论的标注显示 💬
- **原生 PDF 批注**：打开时读取 Edge/WPS 的 Highlight；工具栏「写入 PDF」把标注写成 `/Highlight` + `/Text`（WPS/Edge 可打开）
- **撤销/重做**：PDF 界面 `Ctrl+Z` / `Ctrl+Y` / `Ctrl+Shift+Z` 操作标注栈

## [2.8.2] - 2026-09-17

### PDF 标黄：偏高 + 颜色过艳
- **文字层对齐**：优先用 PDF `item.height × scale` 定位，top 按基线反推字形顶，不再按整倍 em-box 上浮（选区/高亮偏高的主因）
- **颜色更柔**：半透明暖黄 `rgba(255,232,96,.38)`；支持 multiply 时用 `#fff3a0` + `opacity .55`，不再出现实心黄块盖字
- **色带再下移**：新高亮约落在行框下半部（top +30%，高 48%）；旧批注自动重偏

## [2.8.1] - 2026-09-17

### PDF 标黄观感（对齐 Edge / WPS）
- **高亮层改到文字层下方**（渲染顺序：Canvas → 高亮 → 透明文字层），不再像黄块盖在字上
- 使用 `mix-blend-mode: multiply` + 更干净的 `#ffe566`，黑字在支持混合时保持黑色
- 高亮矩形**压到行框下半部**（约 58% 高度、略下移），更像字下的荧光笔
- 旧批注整行高度会自动按同一规则重偏，无需重标
- 选区仍为浅琥珀，与标黄同色系

## [2.8.0] - 2026-09-17

### PDF 阅读体验
- **工具栏改图标**：适应（适应高度）、顺时针旋转、缩放、翻页
- **双页显示**：左右并排两页，翻页按 2 页步进；双页时自动按两页宽度适配
- **Ctrl+滚轮** 缩放显示
- **标黄默认关闭**；颜色固定浅黄；工具栏仅保留标黄开关图标
- **擦除**：去掉独立擦除键——标黄关闭时单击高亮可选中，右键「擦除此高亮」
- **清除**：图标 + **两次确认**
- **去掉复制按钮**（与系统选中右键复制重复）
- **选区观感**：去掉文字层 `opacity:0.25`，改用与高亮同系的**浅琥珀选区**，减轻蓝色与文字层割裂感

## [2.7.1] - 2026-09-17

### 底层二次性能优化
- **Markdown 块 HTML 缓存**：同源块不再重复 `md.render`（切标签 / 撤销 / 大段删除后重绘显著更快）
- **splitMarkdownBlocks 结果缓存**：相同全文不重复切块
- **KaTeX 后处理**：更精确识别 `$…$` / `$$`；前 6 块同步、其余 idle，避免公式文档首屏卡死
- **块编辑 / 撤销路径**：去掉多余的整篇 `splitMarkdownBlocks`，交给增量 diff
- **标签栏事件委托**：关闭/切换/拖拽不再为每个标签绑监听
- **最近文件**：已是队首则不写盘；标签渲染用 DocumentFragment
- **状态栏字数**：撤销后立即刷新；其余仍节流

## [2.7.0] - 2026-09-17

### 性能（阅读模式 + 全局操作）
- **块渲染**：仅当 HTML 含脚本/事件属性时才做消毒，普通段落跳过全树扫描
- **悬停手柄**：命中检测限频约 30fps；拖选/选区仍直接跳过
- **字数统计 / 大纲**：节流；大纲改为事件委托，重建不再绑 N 个监听
- **脏标记**：输入时只修补标签小圆点，不再整条重建标签栏
- **标签标题**：文件夹集合缓存，避免 O(n²)
- **预览调度**：合并重复 scheduleRender；超长文档 debounce 更长
- **查找**：本文件高亮上限 400，避免超大命中卡死
- **文件树 / 最近**：高亮只改前后节点；最近列表事件委托
- **会话写盘**：debounce 加长到 900ms
- **CSS**：`.md-block { contain: layout style }` 缩小重排范围（不用 content-visibility，避免 WebView2 滚动异常）

## [2.6.1] - 2026-09-17

### 新增 / 修复
- **示例文档补全**：恢复完整语法演示、快捷键、PDF、外观与「性能测试段落」；内置 fallback 同步用仓库 `samples/示例文档.md`
- **阅读模式大段选中删除卡顿**（性能）：
  - 拖选时不再每帧做块命中检测（原 `getBoundingClientRect` 全量扫描是主因）
  - 新增预览选区 **Delete/Backspace 快路径**：多块删除只改 Markdown 源一次 + 单次渲染，不再进 contenteditable 逐键重排
  - 大纲重建节流 80ms；悬停 fallback 扫描块数上限
  - 无 editing 时跳过 `getActiveEditingBlock` 清理路径

## [2.6.0] - 2026-09-17

### 修复 / 启动体验
- **去掉启动 cmd 黑框**：注册关联时不再调用 `cmd /C ie4uinit`（2.5.2 为刷新资源管理器引入，观感差）
- **缩短白屏**：高亮 / Mermaid / PDF / 插件 / 音乐脚本改为 `defer`；关联注册与插件加载延后，不挡启动
- **启动不再先闪主页**：用 boot splash 盖住，按策略直达目标页

### 启动策略（按你的要求）
1. **安装后首次**：直接打开示例页（不经过主页）
2. **示例页被关闭后**：下次启动进**主页**（不再自动开示例）
3. **有上次会话**：恢复标签栏打开的文件 + 左侧上次目录
4. 命令行 / 文件关联打开的文件优先
5. 老用户（已有最近文件或目录）迁移时视为已关闭示例，直接进主页/会话

### 其他
- 检查更新的 PowerShell 使用隐藏窗口 + `CREATE_NO_WINDOW`
- 关闭标签 / 打开文件夹 / 切标签 / 关窗口时写入会话（tabs + active + folder + sample_dismissed）

## [2.5.2] - 2026-09-17

### 修复
- **PDF 打开后整页空白**：`openPdf` 只调用了 `fitToWidth(false)` 计算缩放，**没有调用 `renderAll()`**，导致工具栏能显示总页数、内容区却不绘制任何页面
- 打开 PDF 后立即绘制前两页；IntersectionObserver 失效时 400ms 兜底再画
- 占位符阶段不再对每一页重复 `getPage(1)`（长报告更轻）
- 单页渲染失败会显示「第 N 页渲染失败」，不再静默空白
- PDF.js worker 路径按页面 URL 解析，关联启动时更稳

## [2.5.1] - 2026-09-17

### 修复
- **PDF 出现在系统「打开方式」**：此前文件关联只注册了 `.md/.markdown/.mdown/.mkd`，未写入 `.pdf`
- 注册逻辑补充：
  - `OpenWithProgids\StuartMD.PDF`（不抢系统默认 PDF 阅读器）
  - `Applications\stuartmd.exe\SupportedTypes`（.pdf / .md / .txt 等）
  - 独立 ProgID `StuartMD.PDF`
- 安装包增加 `fileAssociations`（md Editor + pdf Viewer）
- 应用启动时自动刷新一次关联（升级后路径变化也能跟上）
- 设置页状态文案分别显示 Markdown / PDF 注册情况

### 使用
- 设置 → 默认打开方式 → **注册 Markdown / PDF 打开方式**
- 对 PDF 右键 → 打开方式 → 选择 StuartMD（若列表未刷新，可稍等或重开资源管理器）

## [2.5.0] - 2026-09-17

### 新增
- **灰色主题**：介于浅色与深色之间；黑底改为中灰，白字压暗，减轻夜间刺眼
- 主题菜单 / 设置 / 三语词条同步加入「灰色 / Gray」

### 变更
- **分栏布局**：左侧渲染内容，右侧 Markdown 源码
- 分栏拖动条按右侧源码宽度调整（20%–75%）

## [2.4.1] - 2026-09-17

### 变更
- **分栏布局**：左侧为渲染内容（预览），右侧为 Markdown 源码（原先左右相反）
- 分栏拖动条改为按右侧源码宽度调整（20%–75%）

## [2.4.0] - 2026-09-17

### 中期目标准备（AI 接口）
- 新增稳定宿主门面 `window.StuartAgent` / `window.Stuart`：文档读写、大纲、块、选区、撤销、工具注册
- 插件 API 增加 `StuartPlugin.registerTool` / `agent()`
- 后端新增：`stuart_get_capabilities`、`ai_home`、`memory_*`、`stuart_search_md`、`stuart_workspace_files`
- AI 记忆目录：`%APPDATA%\StuartMD\ai\memory\`（键名白名单 + 大小限制）
- Agent 写入文档默认清洗 `<script>` / `on*` / `javascript:`

### 安全加固（不改交互）
- `open_url` 仅允许 `http://` / `https://`
- 插件源码读取限制在插件目录内
- 导出 HTML 文件名消毒，阻断 `../` 路径穿越
- 壁纸 base64 体积上限；相对图片路径阻断 `..` 逃逸
- 设置读写加锁；无变更时不再反复写盘
- 预览/ Mermaid SVG 渲染后剥离脚本类节点

### 性能 / 稳定性
- 自动保存竞态：切换文档后不再误标「已保存」
- 打开文档加序号防竞态；文件夹搜索可中断 + 分批 + 优先走后端
- 撤销栈增加约 4MB 字节上限；块编辑 document 监听不再泄漏
- 重建预览后清理失效查找高亮；PDF 切换时销毁 worker
- 目录树遍历节点上限；非 UTF-8 文件不再静默变成空文档

## [2.3.1] - 2026-09-17

### 修复 / 变更
- 块手柄菜单不再遮挡正文：图标栏改为 **4 列 × 3 行**，菜单宽度 220px → **168px**
- **默认页宽**：侧栏与正文间距加大（内容 max-width 780px + 左侧 80px 留白），手柄与菜单更不易压字
- **较宽 / 全宽**：块菜单位置对齐飞书——落在**选中内容（行/段/选区）下方**，避免挡住选中文字
- 默认页宽若左侧空隙不够，菜单也会自动改到选中块下方

## [2.3.0] - 2026-09-17

### 变更
- `Ctrl+Shift+Z` = 撤销（同 Ctrl+Z）；`Ctrl+Shift+Y` = 重做（同 Ctrl+Y）
- 块手柄菜单改到**左侧**，尽量不挡正文，也避开侧栏
- 右上角新增**页宽**：默认 / 较宽 / 全宽（主题与「更多」之间）
- 仓库清理：移除 pywebview/Inno 旧构建目录与入口（`dist/`、`build/`、`main.py` 等），只保留 Tauri 维护线

## [2.2.1] - 2026-09-17

### 修复
- 新增内容可 Ctrl+Z 撤销：变更前状态立即入栈；撤销前先提交未失焦的块编辑
- Ctrl+Y / Ctrl+Shift+Z 重做同步修复；预览强制重建，避免撤销后界面不更新

## [2.2.0] - 2026-09-17

### 新增
- 查找栏明确「搜索」按钮；Enter 首次搜索、再次 Enter 下一个
- 查找范围：本文件 / 同级文件夹（仅 md，限扫文件数与结果数）
- 右键插入菜单：图像、链接引用、分割线、表格、代码块、公式块、段落上下

### 修复
- 查找高亮不再因重复 Enter 膨胀（先清理 mark 再标记，导航复用结果）
- 列表行首 Backspace 先取消分点，不直接并入上一行

## [2.1.0] - 2026-09-17

### 变更
- Mermaid 图表 / 块级 LaTeX 不再显示块手柄
- 代码块支持**单击**进入源码编辑
- 编辑过程中手柄不再强制隐藏，单击后仍可唤起

## [2.0.9] - 2026-09-17

### 修复
- 单击编辑结束后若光标残留，块手柄无法再唤起：仅在真正聚焦编辑时屏蔽手柄；失焦后自动清理残留 .editing

## [2.2.1] - 2026-09-17

### 修复
- 新增内容可 Ctrl+Z 撤销：变更前状态立即入栈；撤销前先提交未失焦的块编辑
- Ctrl+Y / Ctrl+Shift+Z 重做同步修复；预览强制重建，避免撤销后界面不更新

## [2.2.0] - 2026-09-17

### 新增
- 查找栏明确「搜索」按钮；Enter 首次搜索、再次 Enter 下一个
- 查找范围：本文件 / 同级文件夹（仅 md，限扫文件数与结果数）
- 右键插入菜单：图像、链接引用、分割线、表格、代码块、公式块、段落上下

### 修复
- 查找高亮不再因重复 Enter 膨胀（先清理 mark 再标记，导航复用结果）
- 列表行首 Backspace 先取消分点，不直接并入上一行

## [2.1.0] - 2026-09-17

### 变更
- Mermaid 图表 / 块级 LaTeX 不再显示块手柄
- 代码块支持**单击**进入源码编辑
- 编辑过程中手柄不再强制隐藏，单击后仍可唤起

## [2.0.9] - 2026-09-17

### 修复
- 单击编辑结束后残留光标 / editing 状态不再永久压住块手柄
- 无焦点的编辑态会自动提交并恢复渲染，悬停手柄可再次唤起

## [2.0.8] - 2026-09-17

### 变更
- 列表 / 表格手柄按**行**定位（悬停到 li / tr 时对齐该行）
- 选中高亮改为**灰色**，并按主题适配（浅色/深色/羊皮纸/小黄人/壁纸）
- 选中浮动工具栏改到选区**下方**

## [2.0.7] - 2026-09-17

### 修复
- 块手柄移入预览面板并改用 fixed 定位，列表 / 代码 / 公式 / 表格上也能稳定出现

## [2.0.6] - 2026-09-17

### 修复
- 双击代码块 / 行内代码 / LaTeX / Mermaid 可进入源码编辑
- 块手柄命中率：document 级追踪 + elementFromPoint + 垂直带兜底，鼠标所在行必出手柄

## [2.0.5] - 2026-09-17

### 新增
- Ctrl+Z 撤销、Ctrl+Y / Ctrl+Shift+Z 重做（覆盖预览/块编辑/源码）

### 变更
- 块手柄合并为单一 T ⠿ 按钮
- 手柄展开的块菜单：鼠标离开手柄与菜单后自动收起
- 欢迎示例文档随版本同步（版本号 + 完整功能介绍）

## [2.0.4] - 2026-09-17

### 修复
- 双击进源码编辑后失焦立即回渲染（原先内容未变会跳过重绘，一直停在源码）
- 块外点击也会提交编辑
- 悬停手柄改为延迟隐藏，移向手柄的间隙不再消失

## [2.0.3] - 2026-09-17

### 修复
- 悬停手柄：移向手柄的间隙不再消失（近接保护 + 手柄自身 hover）
- 手柄左移，不再遮挡正文
- 出现选中浮动栏时同时隐藏块手柄

## [2.0.2] - 2026-09-17

### 修复 / 体验（飞书对齐）
- 悬停块手柄未绑定导致不显示；改为 T + 拖动点
- 块菜单：类型宫格 + 缩进和对齐子菜单 + 剪切/复制/副本/删除
- 选中高亮改为浅蓝底深色字（深色主题用深蓝底白字），避免白字突兀

## [2.0.1] - 2026-09-17

### 修复
- 大纲点击跳转：预览重建后按标题索引定位当前节点（原先绑到已卸载 DOM，点了不跳）
- 从源码模式跳转时先切分栏，再滚动；目标标题短暂高亮

## [2.0.0] - 2026-09-17

### 变更
- **大版本**：Tauri 为唯一维护线，版本推进至 2.0.0
- **图标**：靛蓝底 + 白字 S + 蓝色强调条；小尺寸 BMP ICO；安装包指定 `installerIcon`
- 安装目录：`%LOCALAPPDATA%\StuartMD`（NSIS currentUser）

## [1.14.0] - 2026-09-15

> 维护策略：**后续只更新维护 Tauri 轨道**；pywebview Stable 停留在 1.13.1。

### 新增（Tauri）
- 最近打开列表：读写文件、打开文件夹自动记录
- 导出 HTML：系统保存对话框 + 写盘
- 新建空窗口、在资源管理器中显示
- 插件启用/禁用持久化（`plugins_disabled`）
- CLI / 文件关联启动：`startup_file` 注入 `get_app_info`
- 打包内置示例与欢迎文档（`resources/samples`）
- NSIS 安装：简体中文/英文、可选语言、当前用户安装

### 修复（Tauri）
- 设置 `schema_version` 迁移与默认值补全（与 Stable 对齐）
- `resolve_asset` 返回 `file://` URI，预览相对路径图片可用
- PDF 打开时附带 sidecar 批注
- 桥接补齐：`get_recents` / `open_path` / `open_new_window` / `export_html` / `set_plugin_enabled` / `clear_wallpaper` / `reveal_in_explorer`
- `withGlobalTauri`：系统打开/保存/文件夹对话框真正可用
- 版本号统一为 1.14.0（此前 Rust 内嵌仍为 1.12.1）

## [1.13.1] - 2026-09-15

### 新增
- 内置欢迎文档：冷启动无指定文件时自动打开功能介绍（含 GitHub 仓库地址）
- 设置 → **检查更新**：查询 GitHub Releases，发现新版本可打开下载页
- 配置 `schema_version` 与自动迁移：升级时完整保留主题、语言、插件、壁纸、最近文件等

### 变更
- 安装向导明确支持**自定义安装路径**（未禁用目录页；静默安装可用 `/DIR=`）
- 版本号策略：功能变更递增，不再长期停留在 1.0.0

### 文档
- 欢迎 Markdown 写入仓库地址与安装说明
- `docs/PLUGINS.md` 插件开发指南

## [1.1.0] - 2026-09-15

### 新增
- 横版 PDF 适配：自动识别、适应宽/高、旋转
- 插件系统（`%APPDATA%\StuartMD\plugins`）与 `docs/PLUGINS.md`
- 壁纸主题：上传图片自动提取主色
- 三语：简体中文 / 繁體中文 / English

## [1.0.0] - 2026-09-15

### 新增
- 首个正式版 StuartMD（原 MiniTypora / Stuart 演进）
- Markdown 阅读 / 分栏 / 源码；公式、Mermaid、代码高亮
- PDF 阅读与标黄批注（竖版）
- 自动保存、多窗口、文件关联、Inno 安装包
- 主题：浅色 / 深色 / 羊皮纸 / 小黄人 / 毛玻璃
- 设置持久化至 `%APPDATA%\StuartMD`
