# StuartMD Tauri shell (P2 scaffold)

Not the default launcher yet. See `../docs/MIGRATION-P2-TAURI.md`.

```powershell
# once Rust is installed
cd tauri\src-tauri
cargo install tauri-cli --locked
cargo tauri dev
```

Frontend is loaded from `../../web` (same UI as pywebview build).
